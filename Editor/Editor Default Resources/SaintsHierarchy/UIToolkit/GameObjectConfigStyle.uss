:root {
}

#IconContainer #unity-content-container {
    flex-wrap: wrap;
    flex-direction: row;
}

.Search {
    flex-grow: 1;
    flex-shrink: 1;
    width: auto;
}

.ColorContainer {
    flex-grow: 1;
    flex-wrap: wrap;
    flex-direction: row;
    flex-shrink: 0;
}
